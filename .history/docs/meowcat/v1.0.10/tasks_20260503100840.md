# v1.0.10 任务拆解 + 进度跟踪

> 来源: `.qoder/plans/meowcat-v1.0.10-roadmap.md` v1.0.10 章节

## 任务清单

### 1. Gateway 协议定义: `gateway/protocol.py`

- [ ] `SignalContext` dataclass（frozen, session_id/platform/user_id/timestamp）
- [ ] `GatewayProtocol` Protocol（start/stop/mount_adapter/unmount_adapter）
- [ ] `AdapterProtocol` Protocol（name/serve/send/stream_chunk/stream_end/stop）

### 2. Gateway 实现: `gateway/__init__.py`

- [ ] `Gateway` 类 — 持有 cat + _adapters dict
- [ ] `mount_adapter(adapter)` — 挂载，同名覆盖
- [ ] `unmount_adapter(name)` — 卸载
- [ ] `start()` — 并行启动所有 Adapter.serve()
- [ ] `stop()` — 停止所有 Adapter
- [ ] `_on_message(text, ctx)` — 注入 cat.perceive()，返回回复
- [ ] `_on_stream(text, ctx)` — 流式版本

### 3. HttpAdapter: `gateway/http_adapter.py`

- [ ] `HttpAdapter` 类（host/port）
- [ ] `serve(on_message, on_stream)` — asyncio HTTP server
- [ ] `send(output, session_id)` — 存 buffer / HTTP response
- [ ] `stream_chunk` / `stream_end` — SSE 推送（可选）
- [ ] `stop()` — 关闭 server

### 4. WsAdapter: `gateway/ws_adapter.py`

- [ ] `WsAdapter` 类（host/port）
- [ ] `serve(on_message, on_stream)` — asyncio WebSocket server
- [ ] 双向帧收发
- [ ] `stream_chunk` / `stream_end`
- [ ] `stop()`

### 5. WebhookAdapter: `gateway/webhook_adapter.py`

- [ ] `WebhookAdapter` 类（host/port/path）
- [ ] `verify_signature(headers, body)` — 可被子类重写
- [ ] `parse_message(body)` — 可被子类重写
- [ ] `serve(on_message, on_stream)` — HTTP POST 接收
- [ ] `stop()`

### 6. CliAdapter: `gateway/cli_adapter.py`

- [ ] `CliAdapter` 类
- [ ] `serve(on_message, on_stream)` — stdin.readline 循环
- [ ] `send` — print()
- [ ] `stream_chunk` / `stream_end` — print(flush=True)
- [ ] `stop()`

### 7. IpcAdapter: `gateway/ipc_adapter.py`

- [ ] `IpcAdapter` 类（socket_path）
- [ ] `serve(on_message, on_stream)` — Unix socket server
- [ ] `send` / `stream_chunk` / `stream_end`
- [ ] `stop()`

### 8. 公开 API 导出: `meowcat/__init__.py`

- [ ] 导出 `Gateway`, `GatewayProtocol`, `AdapterProtocol`, `SignalContext`
- [ ] 导出各 Adapter 类: `HttpAdapter`, `WsAdapter`, `WebhookAdapter`, `CliAdapter`, `IpcAdapter`

### 9. 测试: `tests/test_v1010_gateway.py` (~30 tests)

- [ ] TestSignalContext (3): 构造 / 不可变 / 字段类型
- [ ] TestProtocols (5): GatewayProtocol isinstance / AdapterProtocol isinstance / 缺失方法抛错
- [ ] TestGateway (3): mount / unmount / 同名覆盖
- [ ] TestHttpAdapter (4): POST 收发 / JSON 解析 / 错误响应 / SSE
- [ ] TestWsAdapter (4): 连接 / 消息帧 / 流式帧 / 断开
- [ ] TestCliAdapter (3): stdin 输入 / stdout 输出 / stream 输出
- [ ] TestWebhookAdapter (3): 签名验证 / 消息解析 / 回调
- [ ] TestIpcAdapter (2): 连接 / 收发
- [ ] TestMultiAdapter (3): 两 Adapter 共存 / 独立 session_id / 独立 platform
- [ ] TestSignalContextInjection (2): perceive extras 透传 / Pipeline ctx 可访问

### 10. 版本号

- [ ] `meowcat/pyproject.toml`: 1.0.9 → 1.0.10

---

## 验收清单

- [ ] 所有新增测试通过（~30 个）
- [ ] 已有 635 tests 零回归
- [ ] Gateway + HttpAdapter + CliAdapter 最小可用: 启动 HTTP server + CLI 同时工作
- [ ] SignalContext 通过 perceive extras 正确注入
- [ ] 框架层零 import meowagent
- [ ] 零新增 pip 依赖（纯 asyncio + 标准库）
- [ ] `from meowcat.gateway import Gateway, HttpAdapter, CliAdapter` 可用
- [ ] `from meowcat import Gateway, SignalContext` 可用

---

## 开发顺序

```
1. protocol.py     → 先定协议（SignalContext + 两个 Protocol）
2. __init__.py     → Gateway 实现（依赖协议）
3. http_adapter.py → 最简单的 Adapter，验证 Gateway 集成
4. cli_adapter.py  → 第二简单
5. ws_adapter.py   → 流式支持
6. webhook_adapter.py → 骨架
7. ipc_adapter.py  → 最后
8. __init__.py     → 公开 API 导出
9. tests           → 测试覆盖
```
